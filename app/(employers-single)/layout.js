export const metadata = {
    title: "Company Detail",
    description: "Job Finder - Company Detail",
  };
  
  export default function EmployersSingleV1({ children }) {
    return <>{children}</>;
  }
      